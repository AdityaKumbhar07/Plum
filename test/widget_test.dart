import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads successfully', (WidgetTester tester) async {
    // Basic smoke test — to be expanded later
    expect(true, isTrue);
  });
}
